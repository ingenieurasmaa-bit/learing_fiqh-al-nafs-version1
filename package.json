{
  "name": "fiqhnafs-learning",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:github": "npm run build && gh-pages -d dist",
    "deploy:firebase": "npm run build && firebase deploy",
    "deploy:netlify": "npm run build && netlify deploy --prod",
    "deploy:all": "npm run build && gh-pages -d dist && firebase deploy"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "firebase": "^10.0.0"
  }
}
